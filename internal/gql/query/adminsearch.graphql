query AdminSearch($query: String!) {
  adminSearch(query: $query) {
    nodes {
      ... on UserSearchResult {
        users {
          deletedBy
          email
          displayName
          alias
          profileImage
          lastPostSeenCursor
          awards
        }
      }
    }
  }
}
